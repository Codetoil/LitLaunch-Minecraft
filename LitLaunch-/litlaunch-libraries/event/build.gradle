Project litlaunchLibraryApi = toProject(project, "litlaunch-library-api")

String version1 = "0.1.0+build.0"
String archives1 = litlaunchId + "-event-library"
String name1 = 'LitLaunch Event Library'

if (this.parent.parent != null && this.parent.parent.publishing.publications.getNames().contains("maven-event")) {
    this.parent.parent.publishing.publications.getByName("maven-event").version =
            version1
    this.parent.parent.publishing.publications.getByName("maven-event").artifactId =
            archives1
}

dependencies {
    api litlaunchLibraryApi
}