Project litlaunchLibraryApi = toProject(project, "litlaunch-library-api")

String version1 = "0.1.0+build.0"
String archives1 = litlaunchId + "-dependencies-library"
String name1 = 'LitLaunch Dependencies Library'

if (this.parent.parent != null && this.parent.parent.publishing.publications.getNames().contains("maven-dependencies")) {
    this.parent.parent.publishing.publications.getByName("maven-dependencies").version =
            version1
    this.parent.parent.publishing.publications.getByName("maven-dependencies").artifactId =
            archives1
}

dependencies {
    api litlaunchLibraryApi
}